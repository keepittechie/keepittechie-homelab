# Private Inventory Example

This folder shows how to structure private inventory, but the real private inventory should not be committed.

Recommended pattern:

```bash
inventory/private/
```

Add that path to `.gitignore`.
